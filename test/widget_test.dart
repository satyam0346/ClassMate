// Widget tests for ClassMate.
// The default Flutter boilerplate test has been replaced because
// the app requires Firebase + Riverpod initialisation before pumping.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder — app widget tests require Firebase init',
      (WidgetTester tester) async {
    // Real integration tests are in the integration_test/ directory.
    expect(true, isTrue);
  });
}
